@model IEnumerable<ProyectoFinal.Models.ActivitySchedule>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Activity.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Day)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HourFrom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HourTo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Activity.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Day)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HourFrom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HourTo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ActivityScheduleID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ActivityScheduleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ActivityScheduleID })
        </td>
    </tr>
}

</table>

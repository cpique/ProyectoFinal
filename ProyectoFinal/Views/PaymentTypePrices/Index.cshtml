@model IEnumerable<ProyectoFinal.Models.PaymentTypePrice>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PaymentType.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateFrom)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PaymentType.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateFrom)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PaymentTypePriceID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PaymentTypePriceID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PaymentTypePriceID })
        </td>
    </tr>
}

</table>
